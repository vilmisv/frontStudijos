let userSum1 = Number(prompt('write first number')); 
let userSum2 = Number(prompt ('write second number'));

function addNumbers (sum1, sum2) {
    return sum1 + sum2;
    }
    
alert(`number sum is ${addNumbers(userSum1, userSum2)}`);
